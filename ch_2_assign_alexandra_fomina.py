teams = {}
while True:
    command = input(""" Type add to add a team, 
    	Type remove to remove a team, 
    	Type search to search for a team, 
    	Type list all teams to display all teams, 
    	Type quit to exit
    	""")
    if command == "add" :
        add = input("What is the team number?")
        teams[add] = {}

    if command == quit
        break
#print (""" Type 'add' to add a team
#Type 'information' to see team's information)